framework:
    notifier:
        chatter_transports:
            # Utilisez la passerelle de notification de chat de votre choix, par exemple :
            # slack: '%env(SLACK_DSN)%'
            # telegram: '%env(TELEGRAM_DSN)%'
        texter_transports:
            # Utilisez la passerelle de notification SMS de votre choix, par exemple :
            # twilio: '%env(TWILIO_DSN)%'
            # nexmo: '%env(NEXMO_DSN)%'
        channel_policy:
            # use chat/slack, chat/telegram, sms/twilio or sms/nexmo
            urgent: ['email']
            high: ['email']
            medium: ['email']
            low: ['email']
        admin_recipients:
            - { email: hadir.elayeb@esprit.com }
