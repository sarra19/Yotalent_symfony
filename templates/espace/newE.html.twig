{% extends 'FrontS.html.twig' %}

{% block title %}New Espace Talent{% endblock %}

{% block body %}
    <div class="container my-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="bg-primary text-white p-3 mb-3">
                    <h1 class="text-center mb-0">Create new Espace Talent</h1>
                </div>
                <div class="border rounded border-primary p-3">
                    {{ include('espace/_formE.html.twig') }}
                </div>
                <div class="my-4 text-center">
                    <a href="{{ path('app_espace_frontu') }}" class="btn btn-secondary">Back to list</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
