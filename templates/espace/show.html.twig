{% extends 'base.html.twig' %}

{% block title %}Espacetalent{% endblock %}

{% block body %}
    <h1>Espacetalent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idest</th>
                <td>{{ espacetalent.idest }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ espacetalent.username }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ espacetalent.image }}</td>
            </tr>
            <tr>
                <th>Nbvotes</th>
                <td>{{ espacetalent.nbvotes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_espace_index') }}">back to list</a>

    <a href="{{ path('app_espace_edit', {'idest': espacetalent.idest}) }}">edit</a>

    {{ include('espace/_delete_form.html.twig') }}
{% endblock %}
