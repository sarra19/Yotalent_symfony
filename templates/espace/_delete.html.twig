<form method="post" action="{{ path('app_espace_delete', {'idest': espacetalent.idest}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ espacetalent.idest) }}">
    <button class="btn">Delete</button>
</form>
