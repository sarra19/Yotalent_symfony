{% extends 'Back.html.twig' %}


{% block body %}
   <center> <h1>Edit Voyage</h1> </center>

    {{ include('voyage/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_voyage_index') }}">back to list</a>

    {{ include('voyage/_delete_form.html.twig') }}
{% endblock %}
