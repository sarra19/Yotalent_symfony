{% extends 'Back.html.twig' %}


{% block body %}
    <h1>Voyage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idvoy</th>
                <td>{{ voyage.idvoy }}</td>
            </tr>
            <tr>
                <th>Datedvoy</th>
                <td>{{ voyage.datedvoy }}</td>
            </tr>
            <tr>
                <th>Datervoy</th>
                <td>{{ voyage.datervoy }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ voyage.destination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voyage_index') }}">back to list</a>

    <a href="{{ path('app_voyage_edit', {'idvoy': voyage.idvoy}) }}">edit</a>

    {{ include('voyage/_delete_form.html.twig') }}
{% endblock %}
