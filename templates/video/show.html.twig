{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idvid</th>
                <td>{{ video.idvid }}</td>
            </tr>
            <tr>
                <th>Nomvid</th>
                <td>{{ video.nomvid }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ video.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_video_index') }}">back to list</a>

    <a href="{{ path('app_video_edit', {'idvid': video.idvid}) }}">edit</a>

    {{ include('video/_delete_form.html.twig') }}
{% endblock %}
