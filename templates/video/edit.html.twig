{% extends 'BackS.html.twig' %}

{% block title %}Edit Video{% endblock %}

{% block body %}
<div class="container my-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h1 class="text-center">Edit Video</h1>
                </div>
                <div class="card-body">
                    {{ include('video/_formV.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
            <div class="my-4">
                <a href="{{ path('app_video_index') }}" class="btn btn-secondary">Back to list</a>
                {{ include('video/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
