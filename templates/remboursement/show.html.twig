{% extends 'Back.html.twig' %}

{% block title %}Remboursement{% endblock %}

{% block body %}
    <h1>Remboursement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idrem</th>
                <td>{{ remboursement.idrem }}</td>
            </tr>
            <tr>
                <th>Dc</th>
                <td>{{ remboursement.dc }}</td>
            </tr>
            <tr>
                <th>Idu</th>
                <td>{{ remboursement.idu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_remboursement_index') }}">back to list</a>

    <a href="{{ path('app_remboursement_edit', {'idrem': remboursement.idrem}) }}">edit</a>

    {{ include('remboursement/_delete_form.html.twig') }}
{% endblock %}
