{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    <table>
    <tr>
   <td> {{ form_label(form.nomcat, 'Nom de categorie :') }} </td>
   <td> {{ form_widget(form.nomcat) }} </td>
   <td>{{ form_errors(form.nomcat) }}  </td>

   
</tr>
</table>
    <button class="btn">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
