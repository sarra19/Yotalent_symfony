{% extends 'back.html.twig' %}

{% block title %}Ticket{% endblock %}

{% block body %}
    <h1>Ticket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idt</th>
                <td>{{ ticket.idt }}</td>
            </tr>
            <tr>
                <th>Prixt</th>
                <td>{{ ticket.prixt }}</td>
            </tr>
            <tr>
                <th>Nomev</th>
                <td>{{ ticket.nomev }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ ticket.etat ? '1' : '0' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ticket_index') }}">back to list</a>

    <a href="{{ path('app_ticket_edit', {'idt': ticket.idt}) }}">edit</a>

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
