<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Espaces Talents</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 16px;
            color: #333;
        }
        h1 {
            font-size: 24px;
            margin-bottom: 20px;
            text-align: center;
        }
        table {
            border-collapse: collapse;
            width: 100%;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: center;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
            vertical-align: middle;
            padding: 12px;
        }
        img {
            max-width: 150px;
            max-height: 150px;
        }
        .btn-container {
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .btn {
            padding: 10px 20px;
            margin: 0 5px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            text-transform: uppercase;
            cursor: pointer;
            color: #fff;
        }
        .btn-primary {
            background-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0062cc;
        }
        .btn-success {
            background-color: #28a745;
        }
        .btn-success:hover {
            background-color: #218838;
        }
        .btn-danger {
            background-color: #dc3545;
        }
        .btn-danger:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
    <h1>Liste des Espaces Talents</h1>
    <img src="chemin/vers/votre/image.jpg" alt="description de l'image">
    <table>
        <thead>
            <tr>
              <th scope="col">ID</th>
              <th scope="col">Username</th>
              <th scope="col">Votes</th>
    
            </tr>
        </thead>
        <tbody>
            {% for espacetalent in espacetalents %}
            <tr>
              <td>{{ espacetalent.idest }}</td>
              <td>{{ espacetalent.username }}</td>
              <td>{{ espacetalent.nbvotes }}</td>

             
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
